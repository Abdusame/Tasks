package tasks.innerclasses;

/**
 * Глава 11. Коллекции объектов. Итераторы. 14.
 * Создайте пустой контейнер LinkedList<Integer>.
 * Используя ListIterator, добавьте в List значения Integer;
 * все операции вставки должны осуществляться в середине списка.
 * */
public class LinkedList_ListIterator {
    public static void main(String[] args) {
        /*List<Integer> list = new LinkedList<>();
        ListIterator listIterator = list.listIterator();
        listIterator.add(123);
        listIterator.set(1);*/
        int[] sdf = new int[]{1,2,3,4};
        System.out.println(sdf);
    }
}
